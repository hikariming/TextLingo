# ==========================================
# TextLingo2 后端 API 环境变量配置示例
# ==========================================
# 使用方法：复制此文件为 .env 并填入真实配置

# 应用基础配置
# --------------------------------------------
APP_NAME="TextLingo2 API"
APP_VERSION="1.0.0"
DEBUG=false

# Supabase 配置
# --------------------------------------------
# 从 Supabase Dashboard > Project Settings > API 获取
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-anon-key
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-service-role-key

# JWT 配置
# --------------------------------------------
# 生成强密钥: openssl rand -hex 32
JWT_SECRET_KEY=your-super-secret-jwt-key-32-characters-long
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# 应用安全配置
# --------------------------------------------
# 生成强密钥: openssl rand -hex 32
SECRET_KEY=your-secret-key-here


# API 配置
# --------------------------------------------
API_V1_PREFIX=/api/v1

# CORS 跨域配置 (允许的前端域名)
# --------------------------------------------
# 本地开发环境
CORS_ORIGINS=["http://localhost:3000", "http://127.0.0.1:3000"]

# 生产环境示例 (请根据实际情况修改)
# CORS_ORIGINS='["https://yourdomain.com", "https://www.yourdomain.com"]'

# ==========================================
# 详细配置说明
# ==========================================

# 📋 Supabase 配置获取方法:
# 1. 访问 https://supabase.com/dashboard
# 2. 选择您的项目 (或创建新项目)
# 3. 进入 Settings > API
# 4. 复制以下值:
#    - Project URL → SUPABASE_URL
#    - anon public key → SUPABASE_ANON_KEY  
#    - service_role secret key → SUPABASE_SERVICE_ROLE_KEY

# 🔑 安全密钥生成:
# 在终端运行: openssl rand -hex 32
# 将生成的 64 字符密钥用于 JWT_SECRET_KEY 和 SECRET_KEY

# 🚀 推荐的免费 Redis 服务配置:

# ⭐ Upstash Redis (最推荐)
# 1. 注册: https://console.upstash.com
# 2. 创建数据库 > Global > Free
# 3. 复制 Redis URL 到配置中
# 4. 支持 REST API，非常适合 Serverless

# 🏢 Redis Cloud (官方)
# 1. 注册: https://redis.com/try-free  
# 2. Create subscription > Fixed > Free
# 3. 创建数据库后获取连接信息
# 4. 格式: rediss://default:password@host:port

# 🚂 Railway Redis
# 1. 注册: https://railway.app
# 2. New Project > Add Service > Redis
# 3. 配置相应的环境变量
# 4. 每月 $5 免费额度

# 🎨 Render Redis  
# 1. 注册: https://render.com
# 2. New > Redis
# 3. 选择免费计划
# 4. 获取内部 Redis URL

# 🐳 Docker 本地开发 (如果您想用本地Redis):
# docker run -d --name redis -p 6379:6379 redis:7-alpine

# ==========================================
# 生产环境建议
# ==========================================

# 🔒 生产环境安全设置:
# - 设置 DEBUG=false
# - 使用强密钥 (32+ 字符)
# - 配置正确的 CORS 域名
# - 使用 HTTPS
# - 定期轮换密钥

# 📊 性能优化:
# - Upstash: 最适合 Serverless 部署
# - Redis Cloud: 最适合传统部署
# - Railway/Render: 适合小型项目 

# AI 和 LangChain 配置
GOOGLE_API_KEY=your-gemini-api-key-here
OPENROUTER_API_KEY=your-openrouter-api-key-here
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
DASHSCOPE_API_KEY=your-dashscope-api-key-here

# 默认模型配置
DEFAULT_GEMINI_MODEL=gemini-1.5-flash
DEFAULT_OPENROUTER_MODEL=openai/gpt-3.5-turbo

# AI 服务配置
AI_REQUEST_TIMEOUT=30
MAX_TOKENS=2048
TEMPERATURE=0.7

# 跨版本认证配置
# --------------------------------------------
# TextLingo1 API 地址 (用于跨版本认证)
# 本地开发环境
TEXTLINGO1_API_URL=http://localhost:5001
# 生产环境 (Railway部署)
# TEXTLINGO1_API_URL=https://textlingoclose-production.up.railway.app

# Google OAuth 配置
# --------------------------------------------
# 从 Google Cloud Console > API和服务 > 凭据 获取
GOOGLE_OAUTH_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=your-google-client-secret

# 站点URL配置
# --------------------------------------------
# 前端网站地址，生产环境需要修改为实际域名
SITE_URL=http://localhost:3000

# RevenueCat 配置
# --------------------------------------------
# 从 RevenueCat Dashboard > API Keys 获取
REVENUECAT_SECRET_API_KEY=your-revenuecat-secret-key-here
# RevenueCat Webhook 设置 (可选，用于实时同步购买状态)
REVENUECAT_WEBHOOK_SECRET=your-webhook-secret-here 